{
  "name": "frsms",
  "private": true,
  "version": "2.0.0",
  "description": "FRSMS v2 monolith deployment root -- wires the frontend/ (Vite SPA) and backend/ (Express REST API, run as a Vercel serverless function via api/[...all].ts) into a single Vercel project. See README.md.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.10",
    "typescript": "^5.5.3"
  }
}
